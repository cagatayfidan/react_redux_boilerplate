// import { createSelector } from 'reselect';

// import { initialState } from './reducer';

// const selectHome = state => state.login || initialState;

// const makeSelectHome = () => createSelector(selectHome, subState => subState);

// export { makeSelectHome };
